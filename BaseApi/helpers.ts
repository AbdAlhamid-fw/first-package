import _ from "lodash";

export const readCookies = () => {
    // const cookies = document.cookie.split(";").reduce((cookies, cookie) => {
    //     return {
    //         ...cookies,
    //         [_.trimStart(cookie.split("=")[0])]: cookie.split("=")[1],
    //     };
    // }, {});
    // return cookies;
    return
};

